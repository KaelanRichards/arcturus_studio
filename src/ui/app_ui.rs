// src/ui/app_ui.rs
//
// AppUI integrates with egui to draw panels, menus, and handle user input.
// Over time, add state for selected tool, layer selection, etc.

pub struct AppUI {}

impl AppUI {
    pub fn new() -> Self {
        Self {}
    }

    pub fn draw_ui(&mut self) {
        // TODO: Draw egui-based UI elements here
    }
}
